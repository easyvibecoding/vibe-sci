Below is a draft paper with sections already written. Improve transitions
between sections — where needed, add a single bridging sentence at the start
of a section that references the previous one, or at the end that foreshadows
the next. Do NOT rewrite content, add citations, or change facts.

Return a JSON object wrapped in ```json ... ``` mapping section_key to the
updated LaTeX body. Only include sections you actually changed.

PAPER:
{concat}
